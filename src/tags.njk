---
pagination:
  data: collections
  size: 1
  alias: tag
  filter: posts
permalink: /tags/{{ tag }}/
layout: base
---

{# From https://www.11ty.dev/docs/quicktips/tag-pages/ #} {% block content %}
<h1>Posts tagged with “{{ tag }}”</h1>

<ul>
  {% set taglist = collections[ tag ] %} {% for post in taglist | reverse %}
  <li>
    <a href="{{ post.url }}">{{ post.data.title }}</a> | {{ post.data.date | dateReadable }}
  </li>
  {% endfor %}
</ul>
{% endblock %}
